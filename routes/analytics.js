import express from 'express';
const router = express.Router();

// Esta rota está vazia por enquanto, pois a nova lógica está em kpis.js
// Podemos remover este ficheiro mais tarde se não for mais necessário.

export default router;